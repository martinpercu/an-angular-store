import { VocalsToNumbersPipe } from './vocals-to-numbers.pipe';

describe('VocalsToNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalsToNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
